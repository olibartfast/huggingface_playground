# VideoPrism landed after transformers 5.12; 5.15+ has all three backbones.
transformers>=5.15.0
torch>=2.4.0
numpy>=1.24
huggingface_hub>=0.26
accelerate>=1.0

# PE Video's vision backbone loads through TimmWrapper.
timm>=1.0

# Video decoding. torchcodec is preferred (fastest, used by the HF docs);
# the others are automatic fallbacks in hmdb51.decode_video.
torchcodec>=0.2
av>=12.0
opencv-python-headless>=4.9

# RAR3 extraction for the split archive, without a non-free unrar binary.
libarchive-c>=5.0
